# dependencies
node_modules

# build output
dist
dist-ssr
dist-lib

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# local env
*.local
.env
.env.*
!.env.example

# editors / OS
.idea
.DS_Store

# vite cache
.vite

# demo build scaffolding (content/game.json is the source of truth)
tools/

# raw editor exports — drop your `Export` game.json in export/, then run `pnpm assets`
# (writes the lean content/game.json + public/assets/baked/). Ignore contents, keep the folder.
/export/*
!/export/README.md
